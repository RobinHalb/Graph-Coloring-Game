<Window x:Class="GraphColoringGame.RuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GraphColoringGame"
        mc:Ignorable="d"
        Title="RuleWindow" Height="150" Width="250">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Name ="TextIconGrid" Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,0">
            <TextBlock Name="textBlock1" TextWrapping="Wrap" FontSize="9">
    <Bold>Welcome to the Graph Coloring Game</Bold> <LineBreak/><LineBreak/>
                
  and is geared specifically at integrating <Italic>small</Italic> portions
  of flow content into a UI.
            </TextBlock>
        </Grid>
        <Grid Name ="ButtonGrid"   Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Name="TestButton" Grid.Column="0" Height="20" Width="40"/>
        </Grid>
    </Grid>
</Window>
